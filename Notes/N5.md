# Playwright + JS Web UI Automation - Framework Recap 

## 1. Core JS & PW Concepts
#### Async/Await
* PW actions are asynchronous -> they return Promises.
* Use `async function` and `await` keywords 












#### 1. Browser - `The Test Host`
Think of the browser as the main engine. We launch it once and reuse it for speed.
```js
const browser = await chromium.launch({headless:ture});
```
* Use it when:
   - Setting up before all tests (global-setup.js).
   - Running multiple test context in parallel.
   - Switching between 







